// TODO

const PROJECT_NAME = "TODO";
const PROJECT_DESC = "Todo frontend";

const HOST = import.meta.env.HOST || "http://localhost";
const PORT = import.meta.env.PORT || "3000";

export { PROJECT_NAME, PROJECT_DESC, HOST, PORT };
